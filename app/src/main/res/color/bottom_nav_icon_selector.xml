<?xml version="1.0" encoding="utf-8"?>
<selector xmlns:android="http://schemas.android.com/apk/res/android">
    <!-- Icono seleccionado - Color primario vibrante -->
    <item android:color="@color/brand_color" android:state_checked="true" />
    <item android:color="@color/brand_color" android:state_pressed="true" />
    <item android:color="@color/brand_color" android:state_focused="true" />
    <!-- Icono no seleccionado - Gris suave -->
    <item android:color="@color/text_muted" />
</selector>